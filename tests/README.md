# Tests

Workspace-level integration tests that exercise multiple crates together
(e.g. Planner + Memory + Tools end-to-end). Per-crate unit tests live inside
each crate under `src/` or its own `tests/` directory instead.

Empty for now — added once the Runtime pipeline has real behavior to
integration-test.
